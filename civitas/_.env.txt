# ==========================================
# MY ENGINEERING APP - ENVIRONMENT VARIABLES
# ==========================================
# NEVER commit this file to GitHub. Add ".env" to your .gitignore.

# ------------------------------------------
# 1. CORE APPLICATION CONFIGURATION
# ------------------------------------------
VITE_APP_ENV=production
VITE_APP_NAME="My Engineering App"
# Leave blank for now — it will automatically use whatever URL you're deployed on.
VITE_APP_URL=

VITE_API_URL=
VITE_PAYSTACK_CALLBACK_URL=

# ------------------------------------------
# 2. SUPABASE BACKEND (Authentication & Database)
# ------------------------------------------
VITE_SUPABASE_URL=https://ipvmwcxyhmydjtounqeb.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_359udVL4FrslCJcHvVUH_w_K2NtrUsm

# Only fill this in later, and ONLY on a real server — never in the frontend.
SUPABASE_SERVICE_ROLE_KEY=

# ------------------------------------------
# 3. PAYSTACK GATEWAY CONFIGURATION — fill in when we build real payments
# ------------------------------------------
VITE_PAYSTACK_PUBLIC_KEY=
PAYSTACK_SECRET_KEY=
PAYSTACK_WEBHOOK_SECRET=

# ------------------------------------------
# 4. AI INTEGRATION — fill in if/when you reconnect the AI Council features
# ------------------------------------------
ANTHROPIC_API_KEY=
VITE_ANTHROPIC_MODEL=claude-3-5-sonnet-20241022
GEMINI_API_KEY=

# ------------------------------------------
# 5. MONITORING — optional, add later
# ------------------------------------------
SENTRY_DSN=
LOGROCKET_APP_ID=
GA_MEASUREMENT_ID=
